#include <bits/stdc++.h>
using namespace std;

int main () { 
    list<int> ls ; 

    /* 
    functions that can be done are : 
    1. push_back() 
    2. emplace_back() 
    3. push_front() 
    4. emplace_front()

    and the rest like 
    begin, end, rbegin, rend, clear, insert, size, swap
    */
}